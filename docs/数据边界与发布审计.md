# 数据边界与发布审计

## 可发布

- 六框架适配器、统一 runner、评分算法和测试
- 数据 schema、协议说明、环境锁和复现脚本
- 合成单元测试题面

## 只保留本地

- 父仓库 `questions.json`、正式 Question、gold、aliases、source
- 由父仓库生成的知识快照
- 模型原始回复、trace、token 统计、日志、数据库
- `runs/`、`reports/`、`results/` 和导出表
- 模型密钥、本机绝对路径、OpenCode 状态

`scripts/audit_public_boundary.py` 同时检查已跟踪路径与文本凭据模式。`scripts/export_repro_workspace.py` 仅复制通过审计的已跟踪文件。
